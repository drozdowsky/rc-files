#!/bin/bash
if [ -f ~/builds/aurelius/aurelius.tsv ]; then
    cat ~/builds/aurelius/aurelius.tsv | shuf -n 1 | awk 'BEGIN {FS="\t"}; {print $6}' | fold -w 80 -s
else
    cat "Can't find aurelius.tsv"
fi
